import { Variants } from "framer-motion";

// Gentle upward fade — used for text reveals and section entries.
export const fadeUp: Variants = {
  hidden: { opacity: 0, y: 24 },
  show: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.9, ease: [0.22, 1, 0.36, 1] },
  },
};

// Slower, larger-distance fade for hero / full-bleed imagery.
export const fadeUpLarge: Variants = {
  hidden: { opacity: 0, y: 40 },
  show: {
    opacity: 1,
    y: 0,
    transition: { duration: 1.2, ease: [0.22, 1, 0.36, 1] },
  },
};

// Stagger container for lists of children (grid items, timeline rows).
export const staggerContainer = (stagger = 0.08, delay = 0): Variants => ({
  hidden: {},
  show: {
    transition: {
      staggerChildren: stagger,
      delayChildren: delay,
    },
  },
});

export const fadeIn: Variants = {
  hidden: { opacity: 0 },
  show: { opacity: 1, transition: { duration: 0.6, ease: "easeOut" } },
};

// Reveal-on-scroll defaults, shared across sections.
export const viewportOnce = { once: true, margin: "-80px 0px -80px 0px" };
